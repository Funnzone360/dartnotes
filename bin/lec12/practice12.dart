// list , map ,set , Enum in Dart

/*
void main() {
// A List in Dart is an ordered collection of objects.
  // The objects can be of the same type or different types.

  List numbers = [1, 2, 3, 4, 5];
  List names = ["usman", "Yaqube", "ali"];
  List pointvalues = [3.23, 4.23, 66.90, 12, 980];
  List ofalltypes = [120, "usman", "haider", 2.5678];

  print(numbers);
  print(names);
  print(pointvalues);
  print(ofalltypes);
}
*/

/*
void main() {
  List listproperties = [1, "usman", 3.43, 99, true, 5000];
  print(listproperties.length); //it return the length of list
  print(listproperties.first); //it return first element in the list
  print(listproperties.isEmpty); //check list is empty or not
  print(listproperties.isNotEmpty); //check list is empty is not empty
  print(listproperties.last); //return last element in the list
  print(listproperties.reversed); //REVERSED element in the list
  // print(listproperties.single);//IF It checks if the list has only one element and returns it.
  print(listproperties.singleOrNull);
  print(listproperties.lastOrNull);//give last element 
}

*/

/*
void main(List<String> args) {
  // Initialize a list with some initial elements
  List listfunctions = ["first list elements", "second list elements"];
  
  // Initialize a set with additional elements
  var additionalSet = {"additional", "elements", "here"};
  
  // Add a list as a single element to the list
  listfunctions.add([23, "45"]);
  
  // Add a single integer element to the list
  listfunctions.add(34);
  
  // Add a single element from a list to the list
  listfunctions.addAll([4911]);
  
  // Add all elements from a set to the list
  listfunctions.addAll(additionalSet);
  
  // Insert a single element at a specific index
  listfunctions.insert(3, "add at index");
  
  // Insert all elements from a set at a specific index
  listfunctions.insertAll(2, additionalSet);
  
  // Insert all elements from a list at a specific index
  listfunctions.insertAll(2, ["insertall"]);
  
  // Remove the element at a specific index
  listfunctions.remove(2);
  
  // Remove the element at a specific index
  listfunctions.removeAt(3);
  
  // Remove the last element of the list
  listfunctions.removeLast();
  
  // Print the final list
  print(listfunctions);
}
*/








