//we use abstract keyword.
// Abstract classes have at least one abstract method: dos,t have body void display(); 
//we can,t create abstract class object. 
// Abstract classes can hide the actual implementation of code from the user.
// it provide  common interface for classis.



/*
// Abstract class Animal
abstract class Animal {
  // Abstract method for making a sound
  void makeSound();
}

// Concrete subclass Dog
class Dog extends Animal {
  @override
  void makeSound() {
    print("Woof!");
  }
}

// Concrete subclass Cat
class Cat extends Animal {
  @override
  void makeSound() {
    print("Meow!");
  }
}

// Concrete subclass Bird
class Bird extends Animal {
  @override
  void makeSound() {
    print("Chirp!");
  }
}

void main() {
  // Create instances of different animals
  Animal dog = Dog();
  Animal cat = Cat();
  Animal bird = Bird();

  // Use the common interface to make each animal sound
  dog.makeSound(); // Output: Woof!
  cat.makeSound(); // Output: Meow!
  bird.makeSound(); // Output: Chirp!
}
*/


